-- ====================================================================
-- Migration 001 : Schéma initial
-- ====================================================================
-- Cette migration représente le schéma synd_gest.sql déjà en place.
-- Elle sera marquée comme "appliquée" automatiquement lors de la
-- première exécution de /admin/migrate (pas besoin de la ré-exécuter).
--
-- Pour un nouveau déploiement from scratch, importer d'abord synd_gest.sql
-- puis lancer /admin/migrate pour appliquer les migrations suivantes.
-- ====================================================================

SELECT 1;
